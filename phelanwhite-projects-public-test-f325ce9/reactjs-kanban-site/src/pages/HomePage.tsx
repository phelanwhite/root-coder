import Board from "@/components/Board";
import EditorReact from "@/components/form/Editor";

const HomePage = () => {
  return (
    <div>
      <EditorReact />
      <Board />
    </div>
  );
};

export default HomePage;
